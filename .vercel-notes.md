# Vercel Build Configuration

Build command updated to skip migrations during build.
Migrations are managed manually via `prisma migrate deploy` to production database.

This prevents database connection timeouts during Vercel build process.
# Trigger rebuild - Mon Nov  3 17:54:10 CET 2025
